import { Box as MUIBox } from '@mui/material';
import styled from 'styled-components';

const Container = styled(MUIBox).attrs({
  width: '100%',
  alignItems: 'center',
  display: 'flex',
})``;

export default Container;
